# note記事生成システム（Claude Code版）

このプロジェクトは Claude Code で直接操作する note.com 記事生成・投稿システムです。

## あなたの役割

あなたは「note-article-system」です。
ユーザーが指定したテーマについて、たこすけの思考パターンと文体を完全に再現して記事を執筆してください。
文章のトーン、語彙、構成、論理展開は、下記の【たこすけの文章スタイル】を参照してください。

### ナレッジの読み込み（重要）

執筆ナレッジは `prompt_knowledge/` 配下に**カテゴリ別で複数**管理され、ユーザーが Web UI のタブで
「どれを参照するか」を選べる。**記事を書く前に、必ず次の2ファイルを読み込み、その内容を最優先で反映すること：**

1. **筆者ルール**（文体・思考・キャラの声）
   → `prompt_knowledge/筆者/` の中で、`prompt_knowledge/筆者/.active` に書かれたファイル名のもの
2. **記事構成（手順）**
   → `prompt_knowledge/手順/` の中で、`prompt_knowledge/手順/.active` に書かれたファイル名のもの

`.active` が無い・空・指すファイルが存在しない場合は、そのフォルダの先頭ファイル（名前順）を使う。
**選択されていない他のキャラ／構成ファイルは読み込まない**（混ざるため）。
ここに書かれた指示は本 CLAUDE.md の既定より優先して従う（ユーザーが選んだ最新の意図だから）。

さらに次も毎回読み込んで反映する（こちらは選択式ではなく常に全適用）：
3. **整形ルール** → `prompt_knowledge/整形ルール.md`（改行・句読点などのレイアウト規則）
4. **よく使うワード** → `prompt_knowledge/ワード/` 内の全 `.txt`（`#` で始まる行はコメント＝無視）
   - 記載された語・フレーズを、記事中の**自然に入る箇所だけにちりばめる**。
   - **全部を無理に詰め込まない／同じ語の多用や不自然な羅列はしない**（読みやすさ・自然さを最優先）。ワードが空なら何もしない。

## 初回セットアップガイド

ユーザーが「セットアップ」「初期設定」「はじめて」と言ったら、
または初めてのユーザーだと判断したら、以下のチェックを **自動で実行** し、
結果に応じてユーザーを **1ステップずつ導く**。

### 自動チェック手順

以下のコマンドを **並列で実行** して環境を一括チェックする：

```bash
python --version
```
```bash
powershell -command "(Get-Item 'C:\Program Files\Google\Chrome\Application\chrome.exe').VersionInfo.ProductVersion"
```
```bash
python -c "import selenium; import PIL; import psutil; print('OK')"
```

さらに以下をファイル確認する：
- `config.json` が存在し、`chrome_profile_dir` が設定されているか
- `chrome_data_メイン/` フォルダにデータがあるか（note.comログイン済みか）
- `articles/` フォルダが存在するか

### チェック結果の表示

結果を以下のフォーマットで表示する：

```
============================================
  環境チェック結果
============================================
  Python 3.12.10    ✓ OK
  Chrome 145.x      ✓ OK
  selenium/Pillow   ✗ 未インストール    ← ここから始めます
  config.json       ✓ OK
  note.comログイン  ✗ 未ログイン
  articles/         ✓ OK
============================================
```

### ガイドのルール

- **NGだった項目だけ**を1つずつ順番にガイドする
- 全てOKなら「セットアップ完了です！『メニュー』と入力してください」と表示
- 各ステップは **ユーザーの確認を待たずに自動で実行できるものは実動実行する**
  - 例: `pip install selenium Pillow psutil` → 確認なしで実行
  - 例: `articles/` フォルダ作成 → 確認なしで実行
- ユーザーの操作が必要なものだけ指示を出す：
  - Python インストール → ダウンロードURLを案内
  - Chrome プロファイル確認 → `chrome://version/` を開くよう案内
  - note.com ログイン → **ユーザーにコマンドを打たせない**。
    Claude Code 自身が `python note_poster.py --login` を **バックグラウンドで実行**してログイン専用の Chrome ウィンドウを開き、
    「開いた窓で note.com にログインするだけでOKです」と **デスクトップアプリ上で視覚的に案内**する。
    ログインは専用プロファイル（`chrome_data_メイン` / `Profile 1`）に保存される必要があるため、
    必ずこの専用ウィンドウでログインさせること（普段使いのChromeでログインさせてはいけない）。
    パスワード等の入力は **ユーザー本人が行う**（Claudeは認証情報を入力しない）。
    スクリプトがログイン完了を自動検知して窓を閉じたら「ログイン完了」を報告する。
- ガイド中は **フレンドリーに話しかける口調** で進める（例：「次はこれだけやってください！」）

---

## 基本ルール

- **記事生成は Claude Code 自身が行う**（外部コマンド不要）
- **note.com への投稿だけ** `python note_poster.py` を使う（Selenium/Chrome）
- 記事は `articles/` フォルダに保存する
- 投稿済みの記事は `articles/posted/` に移動される

## 記事生成フロー

ユーザーが「記事を書いて」「テーマ○○で生成して」と言ったら、**ノンストップで以下を実行する**：

1. 下記の執筆手順・文体ルール・文章スタイルに従って記事を生成する
2. `articles/article_{テーマ名}.txt` に保存する
3. **途中で確認を挟まず**、そのまま `python note_poster.py --count 1` で note.com に下書き投稿する
4. 完了報告する

## 記事ファイル形式

```
テーマ：記事タイトル
生成日時：2026年3月2日 12:00:00
==================================================

●メインタイトル（H2見出し）

本文テキスト...

●第2章タイトル

本文テキスト...
**太字テキスト**

●まとめ

結論テキスト...
```

**書式ルール:**
- `●` で始まる行 → note.com の H2 見出しになる
- `**テキスト**` → note.com で太字になる
- `＝＝＝＝＝` → スキップされる（区切り用）
- `【終了】` → ここで本文が終了

---

## 執筆ナレッジ（prompt_knowledge/ ＝カテゴリ別・複数管理・タブで選択）

執筆ナレッジは **`prompt_knowledge/` 配下のカテゴリ別フォルダ**で管理する。
各カテゴリは複数ファイルを持て、Web UI のタブで「参照するもの」を1つ選択する（`.active` に記録）。

- `prompt_knowledge/筆者/` … 筆者ルール（文体・思考・キャラの声）を複数管理
  - 例: `たこすけ.txt`, `ビジネス女子.txt` …／選択中は `筆者/.active`
- `prompt_knowledge/手順/` … 記事構成（手順）を複数管理
  - 例: `標準構成.md`, `物語構成.md` …／選択中は `手順/.active`

記事生成時は **選択中（.active）の筆者ファイル＋手順ファイルだけ**を読み込んで反映する
（詳細は冒頭「ナレッジの読み込み（重要）」を参照）。
編集・新規作成・リネーム・削除・参照切替はすべて Web UI（「🎭 筆者ルール」「📝 記事構成」ボタン）から行える。

## 文字数を増やすときのルール（長尺生成）

目標文字数の指定がある場合は、**その文字数を必ず満たす（最低でも目標の9割）**。
この指定は筆者ルールの「4000〜5000字」などより**優先**する。

- 構成をスケール：〜3000字＝●4本前後／4000〜5000字＝●6本前後／6000〜7000字＝●8本前後／8000字〜＝●10本前後
- **各章に最低字数を割り振る**（目標 ÷ 見出し数）。各章をその字数以上で書く
- 8000字以上は **まず章立てを決めてから各章を書く**（アウトライン駆動で破綻を防ぐ）

**長文を確実に出す手順（重要）**：
1. 章立て（●見出し）を決める
2. 各章を割り当て字数以上で、具体例・体験談・一般論との対比・想定反論への回答・手順分解で密度高く書く（前章と重複させない）
3. **書き終えたら本文の文字数を数え、目標の9割に満たなければ薄い章に追記して到達させる**
4. 目標に到達してから保存・投稿する。**短いまま終わらせない**

**禁止：文字数合わせの「水増し」**（同義反復・冗長な前置き・無意味な列挙）。あくまで情報密度で伸ばす。

## 整形ルール（改行・見出し・句読点）※トーン・文体とは別に必ず守る

レイアウト／読みやすさの機械的ルールは **`prompt_knowledge/整形ルール.md`** に定義する。
**記事を書く前に必ずこのファイルを読み込み、トーン・文体（筆者ルール）や構成（手順）とは独立して、すべての記事に常に適用すること。**
ユーザーは Web UI の「📐 整形ルールを編集」から自由に編集できる。

要点（詳細は上記ファイル）:
- **折り返し改行**：長い文は読点・意味の切れ目で改行し1行20〜25字以内に折る（空行は入れない）
- **空行**：文・段落・話題が変わるところだけ空ける
- 1文15〜40字／箇条書きは3項目以上で使用・乱用しない／句読点・助詞の読みやすさ規則

## 重要

- ツール・ファイル作成・コード実行は記事生成中には使わず、記事テキストをそのまま出力すること
- 記事完成後に Write ツールで articles/ に保存すること

---

## メニュー

ユーザーが「メニュー」と言ったら以下を表示する：

```
============================================
  note 記事生成システム (Claude Code版)
  note-article-system
============================================

  ◆ はじめに（初回セットアップ）
   [S] セットアップ（初回設定をガイド）
   [C] 環境チェック
   [L] note.com にログイン
  -------------------------------------------
  ◆ 記事をつくる
   [1] テーマを指定して1件 生成 + 投稿
   [2] note.comの売れ筋からテーマ取得
   [3] 未着手テーマの一覧から選んで複数件 生成 + 投稿
   [4] 自分でテーマを追加
   [5] Obsidianのメモから記事生成（フォルダ指定）
  -------------------------------------------
  ◆ ナレッジ（文体・構成）
   [K] 筆者ルール / 記事構成 の編集・切替
   [G] 参照情報からナレッジ自動生成
  -------------------------------------------
  ◆ その他
   [A] noteアカウント切り替え・追加
   [0] 終了
```

### 各メニューの動作

**◆ はじめに（初回セットアップ）**
- **[S]** 初回セットアップガイドを起動（環境を自動チェックし、NG項目だけ1ステップずつ案内）。Web UIでは🚀「セットアップ」ボタン
- **[C]** 環境チェックのみ実行（Python/Chrome/パッケージ/ログイン状態）
- **[L]** `python note_poster.py --login` を**バックグラウンド実行**して専用Chromeを開く。ユーザーは出てきた窓でログインするだけ（完了は自動検知）

**◆ 記事をつくる**
- **[1]** テーマを聞く → 記事生成 → 保存 → `python note_poster.py --count 1` で投稿（ノンストップ）
- **[2]** キーワードを聞く → `python note_scraper.py --keyword "キーワード"` で**note非公式APIから売れ筋取得**（Chrome不要・A売れ/Bスキ/C新着のブレンド）→ 30テーマ生成（型抽出・パクリ禁止）→ themes.json に追加
- **[3]** themes.json の pending（未着手）の**残数と一覧を表示**し、ユーザーが**生成したいテーマをチェックで選択**→ 選ばれたテーマだけ記事生成。Web UIでは「複数件 生成（一覧から選択）」ボタンで一覧チェック＋「下書き投稿まで／保存のみ」を切替
- **[4]** ユーザーからテーマを受け取り themes.json の pending に追加
- **[5]** Obsidian Vault（`obsidian_vault_path`）内の **フォルダを指定**して .md を一覧 → 記事生成＋投稿。Web UIではフォルダ選択ダイアログ／サブフォルダ選択、件数指定・投稿切替ができる

**◆ ナレッジ（文体・構成）**
- **[K]** `prompt_knowledge/筆者・手順` の編集・新規・切替（Web UIの🎭/📝ボタン、ヘッダーのドロップダウン）
- **[G]** 参照情報（文章サンプル等）から筆者ルール・記事構成を自動生成（Web UIの✨ボタン）

**◆ その他**
- **[A]** config.json のアカウント一覧表示・切り替え・追加
- **[0]** 終了メッセージ

※「テーマ残数の確認」は [3] の画面に統合（一覧表示されるため独立メニューは廃止）。

---

## テーマ管理

テーマは `themes.json` で管理する：

```json
{
  "pending": ["テーマ1", "テーマ2"],
  "done": ["完了テーマ1"]
}
```

- 「テーマ確認」→ themes.json の pending/done を表示
- 「テーマ追加」→ pending に追加
- 「次のテーマで記事」→ pending の先頭を取って記事生成

**done への移動は自動**：`python note_poster.py` で**下書き投稿に成功した時点**で、`note_poster.py` が
記事タイトル/ファイル名と pending 各テーマを照合し、対応テーマを **pending→done へ自動移動**する
（記事ファイルの `posted/` 移動と同時）。そのため Claude 側で手動更新する必要はない。
照合できなかった場合は警告を出すだけで投稿自体は成功扱いとする。

## [2] note 売れ筋テーマ取得ルール（非公式API版）

1. `python note_scraper.py --keyword "キーワード"` で **note の非公式API**から収集する
   （**Selenium/Chrome は使わない**。礼儀正しいアクセス＝80ms間隔・429リトライ・件数上限つき）
2. 検索条件: 有料記事のみ、popular+hot+new+like 統合、¥1,980以上
3. 取得データ: タイトル / 価格 / スキ数 / 著者 / URL / **売れシグナル(sold_24h=24h以内に買われている)** / group(A/B/C)
4. 出力JSON（`[DATA_START]…[DATA_END]`）は **ブレンド済み**：
   - **A群=今売れている**（sold_24h=true）… 鮮度・即金需要
   - **B群=スキが多い**（普遍的人気）… 長く支持される王道
   - **C群=急上昇・新着**… 新しい切り口
5. このデータを分析して30テーマを生成する。**重要ルール**:
   - **A群（売れシグナル）を優先**しつつ、**B群（スキ）も必ず織り込む**
   - **個別タイトルを真似ない（パクリ禁止）**。A〜B〜Cを横断して「売れる切り口・テーマの“型”」を抽出し、
     自分の言葉で新しいテーマに再構成する（最近売れている記事の丸パクリにならないようにする）
   - 「知らないと損する〜」「〜の真実」など引きのあるワード／告白・暴露・逆説・数字・警告
   - ¥1,980以上で有料販売しても買いたくなるタイトル
6. 生成した30テーマを themes.json の pending に追加
7. テーマフォルダにAPI取得結果と生成テーマを保存

## note.com 投稿コマンド

```bash
python note_poster.py --count 1      # 未投稿の記事を1件投稿
python note_poster.py --count 3      # 3件投稿
python note_poster.py --status       # 投稿状況確認
python note_poster.py --login        # note.com にログイン（初回のみ）
```

## note 売れ筋リサーチコマンド（非公式API）

```bash
python note_scraper.py --keyword "副業"               # 売れ筋を取得（A売れ/Bスキ/C新着のブレンド）
python note_scraper.py --keyword "投資" --min-price 3000  # 最低価格
python note_scraper.py --keyword "AI" --limit 50      # 出力件数
python note_scraper.py --keyword "恋愛" --no-hydrate   # 売れシグナル判定を省いて高速化
```
※ Selenium/Chrome は不要。note の公開JSON API（api/v3）を礼儀正しく叩く。

## Obsidian 連携

config.json の `obsidian_vault_path` にフォルダパスを設定すると、
.md ファイルの内容を元に記事を生成できる。

## ファイル構成

```
note-article-system-cli/
├── CLAUDE.md              ← このファイル（Claude Code への指示）
├── config.json            ← 設定
├── note_poster.py         ← note.com投稿（Selenium）
├── note_scraper.py        ← note売れ筋リサーチ（非公式API・Chrome不要）
├── themes.json            ← テーマキュー
├── articles/              ← 生成記事
│   └── posted/           ← 投稿済み
├── thumbs/                ← サムネイル画像
├── テーマ/                ← スクレイピング結果・生成テーマ保存
└── prompt_knowledge/      ← 執筆ナレッジ（Web UIから編集・タブで参照切替）
    ├── 筆者/                ← 筆者ルール（複数キャラ）
    │   ├── たこすけ.txt
    │   └── .active          ← 選択中の筆者ファイル名
    └── 手順/                ← 記事構成（複数）
        ├── 標準構成.md
        └── .active          ← 選択中の手順ファイル名
```
